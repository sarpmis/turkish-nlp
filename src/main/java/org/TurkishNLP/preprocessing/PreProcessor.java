package org.TurkishNLP.preprocessing;

import zemberek.morphology.TurkishMorphology;

public class PreProcessor {
    public TurkishMorphology morphology;
}

// result.bestAnalysis().forEach(getDictionaryItem())
